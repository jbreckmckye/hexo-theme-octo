<% if (page.tags && page.tags.length){ %>
    <span class="categories">
    <% page.tags.forEach((tag, i) => { %>
        <a class="category" href="<%=tag.path%>"><%=tag.name%></a><%= i+1 < page.tags.length ? ',' : ''%>
    <% }) %>
    </span>
<% } %>